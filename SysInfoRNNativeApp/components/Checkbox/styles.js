import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  btn: {
    height: 24,
    width: 24,
    marginRight: 8,
  },
});

export default styles;
